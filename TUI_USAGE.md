# MCP Memory System TUI 使用指南

TUI (Text User Interface) 是一个基于命令行的交互式界面，为AI智能体提供直观的记忆系统操作体验。

## 安装要求

确保已安装所有依赖：

```bash
pip install textual>=0.47.0 rich>=13.7.0 click>=8.1.7
```

## 启动方式

### 基本启动
```bash
memory tui
```

### 指定用户
```bash
memory tui --user your_username
```

### 指定项目
```bash
memory tui --user your_username --project your_project_id
```

### 全屏模式
```bash
memory tui -f
```

### 调试模式
```bash
memory tui --debug
```

## 快捷键

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `q` | 退出应用 | 退出TUI应用 |
| `n` | 新建记忆 | 快速创建新记忆 |
| `s` | 搜索记忆 | 打开搜索界面 |
| `r` | 刷新列表 | 重新加载记忆列表 |
| `h` | 显示帮助 | 查看帮助信息 |
| `f` | 切换全屏 | 全屏/退出全屏 |

## 基本操作

### 1. 导航界面
- 使用方向键在列表中导航
- 按 `Enter` 键选择记忆
- 按 `Tab` 键切换UI焦点
- 按 `Esc` 键返回上一级

### 2. 查看记忆
1. 在主界面记忆列表中选择要查看的记忆
2. 按 `Enter` 或双击进入详情页面
3. 查看记忆的完整信息，包括标题、内容、标签等

### 3. 创建记忆
1. 按 `n` 键或点击"新建"按钮
2. 填写记忆信息：
   - 标题（必填）
   - 内容（必填）
   - 类型（可选：笔记、任务、想法、会议、代码）
   - 关键词（可选，用逗号分隔）
   - 标签（可选，用逗号分隔）
   - 自动增强（默认开启）
3. 点击"保存"按钮

### 4. 搜索记忆
1. 按 `s` 键或点击"搜索"按钮
2. 在搜索框输入关键词
3. 按 `Enter` 执行搜索
4. 搜索结果将显示在下方的列表中

### 5. 编辑记忆
1. 在记忆详情页面点击"编辑"按钮
2. 修改相关信息
3. 点击"保存"按钮

### 6. 删除记忆
1. 在记忆详情页面点击"删除"按钮
2. 确认删除操作

## 高级功能

### 智能搜索
- 支持按内容类型过滤
- 支持按时间范围筛选
- 支持按重要性筛选
- 保存常用搜索查询

### 批量操作
- 批量选择记忆
- 批量删除记忆
- 批量导出记忆
- 批量编辑标签

### AI助手
- 智能记忆总结
- 关联记忆搜索
- 灵感生成
- 智能问答

### 记忆分析
- 生成记忆报告
- 记忆关联分析
- 记忆模式识别
- 记忆健康度评估

### 备份与恢复
- 创建记忆备份
- 查看备份历史
- 恢复备份
- 删除备份

## 界面布局

### 主界面
- **左侧面板**: 用户信息、项目信息、统计信息、操作按钮
- **右侧区域**: 记忆列表、详情查看、标签页切换

### 新建记忆界面
- 标题输入框
- 内容文本区域
- 类型选择器
- 关键词输入
- 标签输入
- 自动增强开关

### 搜索界面
- 搜索输入框
- 高级搜索选项
- 搜索结果列表

## 配置文件

TUI会读取以下配置：
- `MCP_MEMORY_USER`: 当前用户ID（默认: cli_user）
- `MCP_MEMORY_PROJECT_ID`: 当前项目ID
- `MCP_MEMORY_PORT`: 服务器端口
- `MCP_MEMORY_SHARED`: 是否共享记忆

## 常见问题

### 1. 无法启动TUI
确保已安装textual和rich：
```bash
pip install textual rich
```

### 2. 界面显示异常
- 尝试调整终端窗口大小
- 确保终端支持Unicode
- 尝试使用不同的终端模拟器

### 3. 搜索功能不工作
- 检查记忆是否已正确保存
- 确认搜索关键词拼写
- 尝试使用更简单的搜索词

### 4. AI助手响应慢
- 检查网络连接
- 尝试简化问题
- 在调试模式下查看错误信息

## 示例使用场景

### 场景1：日常工作记录
```bash
# 启动TUI
memory tui --user alice --project work

# 创建工作记忆
# 1. 按 n 键新建
# 2. 填写任务详情
# 3. 保存
```

### 场景2：项目知识管理
```bash
# 切换到项目
memory tui --user bob --project myproject

# 搜索相关记忆
# 1. 按 s 键搜索
# 2. 输入关键词
# 3. 查看结果
```

### 场景3：个人笔记整理
```bash
# 查看今日所有笔记
# 1. 按 s 键搜索
# 2. 时间范围选择"今天"
# 3. 查看所有笔记

# 使用AI助手总结
# 1. 点击AI助手按钮
# 2. 选择"总结今日记忆"
```

## 故障排除

### 检查依赖
```bash
memory tui check-dependencies
```

### 查看版本
```bash
memory tui version
```

### 调试模式
```bash
memory tui --debug
```

## 技术支持

如有问题或建议，请通过以下方式获取支持：
1. 查看项目README
2. 检查错误日志
3. 提交Issue到项目仓库
4. 联系开发团队